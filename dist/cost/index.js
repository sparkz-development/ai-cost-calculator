"use strict";
/**
 *
 * DO NOT EDIT THIS FILE UNLESS IT IS IN /costs/src/index.ts
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.getUsageProcessor = exports.modelCostBreakdownFromRegistry = exports.providers = void 0;
exports.costOf = costOf;
exports.costOfPrompt = costOfPrompt;
exports.clickhouseModelFilter = clickhouseModelFilter;
exports.clickhousePriceCalcNonAggregated = clickhousePriceCalcNonAggregated;
exports.clickhousePriceCalc = clickhousePriceCalc;
const mappings_1 = require("./providers/mappings");
const costCalc_1 = require("./costCalc");
var mappings_2 = require("./providers/mappings");
Object.defineProperty(exports, "providers", { enumerable: true, get: function () { return mappings_2.providers; } });
var costCalc_2 = require("./costCalc");
Object.defineProperty(exports, "modelCostBreakdownFromRegistry", { enumerable: true, get: function () { return costCalc_2.modelCostBreakdownFromRegistry; } });
var getUsageProcessor_1 = require("./usage/getUsageProcessor");
Object.defineProperty(exports, "getUsageProcessor", { enumerable: true, get: function () { return getUsageProcessor_1.getUsageProcessor; } });
function costOf({ model, provider, }) {
    var _a;
    const modelLower = model === null || model === void 0 ? void 0 : model.toLowerCase();
    if (!modelLower) {
        return null;
    }
    const providerCost = mappings_1.providers.find((p) => {
        return (p.provider === provider ||
            (p.pattern && p.pattern.test(provider) ? true : false));
    });
    if (!providerCost || !providerCost.costs) {
        return null;
    }
    const cost = providerCost.costs.find((cost) => {
        const valueLower = cost.model.value.toLowerCase();
        if (cost.model.operator === "equals") {
            return valueLower === modelLower;
        }
        else if (cost.model.operator === "startsWith") {
            return modelLower.startsWith(valueLower);
        }
        else if (cost.model.operator === "includes") {
            return modelLower.includes(valueLower);
        }
    });
    return (_a = cost === null || cost === void 0 ? void 0 : cost.cost) !== null && _a !== void 0 ? _a : null;
}
function costOfPrompt({ provider, model, promptTokens, promptCacheWriteTokens, promptCacheReadTokens, promptAudioTokens, completionTokens, completionAudioTokens, promptCacheWrite5m, promptCacheWrite1h, images = 1, perCall = 1, multiple, }) {
    var _a, _b;
    const cost = costOf({ model, provider });
    if (!cost) {
        return null;
    }
    let totalCost = 0;
    // Add cost for regular prompt tokens (these are the fresh, uncached tokens)
    totalCost += promptTokens * cost.prompt_token;
    // Add cost for cache write tokens if applicable
    if (cost.prompt_cache_write_token && promptCacheWriteTokens > 0) {
        // For anthropic requests, the prompt cache write tokens are the sum of the 5m and 1h writes
        // so we subtract to not double count
        const effectivePromptCacheWriteTokens = promptCacheWriteTokens -
            (promptCacheWrite5m !== null && promptCacheWrite5m !== void 0 ? promptCacheWrite5m : 0) -
            (promptCacheWrite1h !== null && promptCacheWrite1h !== void 0 ? promptCacheWrite1h : 0);
        totalCost += effectivePromptCacheWriteTokens * cost.prompt_cache_write_token;
        if (cost.prompt_cache_creation_5m && promptCacheWrite5m && promptCacheWrite5m > 0) {
            totalCost += promptCacheWrite5m * cost.prompt_cache_creation_5m;
        }
        if (cost.prompt_cache_creation_1h && promptCacheWrite1h && promptCacheWrite1h > 0) {
            totalCost += promptCacheWrite1h * cost.prompt_cache_creation_1h;
        }
    }
    else if (promptCacheWriteTokens > 0) {
        totalCost += promptCacheWriteTokens * cost.prompt_token;
    }
    // Add cost for cache read tokens if applicable
    if (cost.prompt_cache_read_token && promptCacheReadTokens > 0) {
        totalCost += promptCacheReadTokens * cost.prompt_cache_read_token;
    }
    else if (promptCacheReadTokens > 0) {
        totalCost += promptCacheReadTokens * cost.prompt_token;
    }
    // Add cost for prompt audio tokens if applicable
    if (cost.prompt_audio_token && promptAudioTokens > 0) {
        totalCost += promptAudioTokens * cost.prompt_audio_token;
    }
    else if (promptAudioTokens > 0) {
        totalCost += promptAudioTokens * cost.prompt_token;
    }
    // Add cost for completion tokens
    totalCost += completionTokens * cost.completion_token;
    // Add cost for completion audio tokens if applicable
    if (cost.completion_audio_token && completionAudioTokens > 0) {
        totalCost += completionAudioTokens * cost.completion_audio_token;
    }
    else if (completionAudioTokens > 0) {
        totalCost += completionAudioTokens * cost.completion_token;
    }
    // Add cost for images and per-call fees
    const imageCost = images * ((_a = cost.per_image) !== null && _a !== void 0 ? _a : 0);
    const perCallCost = perCall * ((_b = cost.per_call) !== null && _b !== void 0 ? _b : 0);
    totalCost += imageCost + perCallCost;
    if (multiple !== undefined) {
        return Math.round(totalCost * multiple);
    }
    return totalCost;
}
function caseForCost(costs, table, multiple, useDefaultCost = false, optimized = false) {
    const validCases = costs.map((cost) => {
        var _a, _b, _c, _d, _e, _f;
        const costPerMultiple = {
            prompt: Math.round(cost.cost.prompt_token * multiple),
            completion: Math.round(cost.cost.completion_token * multiple),
            prompt_audio: Math.round(((_a = cost.cost.prompt_audio_token) !== null && _a !== void 0 ? _a : cost.cost.prompt_token) * multiple),
            completion_audio: Math.round(((_b = cost.cost.completion_audio_token) !== null && _b !== void 0 ? _b : cost.cost.completion_token) *
                multiple),
            prompt_cache_write: Math.round(((_c = cost.cost.prompt_cache_write_token) !== null && _c !== void 0 ? _c : cost.cost.prompt_token) *
                multiple),
            prompt_cache_read: Math.round(((_d = cost.cost.prompt_cache_read_token) !== null && _d !== void 0 ? _d : cost.cost.prompt_token) * multiple),
            image: Math.round(((_e = cost.cost.per_image) !== null && _e !== void 0 ? _e : 0) * multiple),
            per_call: Math.round(((_f = cost.cost.per_call) !== null && _f !== void 0 ? _f : 0) * multiple),
        };
        const costParts = [];
        if (costPerMultiple.prompt > 0) {
            costParts.push(`${costPerMultiple.prompt} * ${table}.prompt_tokens`);
        }
        if (costPerMultiple.completion > 0) {
            costParts.push(`${costPerMultiple.completion} * ${table}.completion_tokens`);
        }
        if (costPerMultiple.prompt_audio > 0) {
            costParts.push(`${costPerMultiple.prompt_audio} * ${table}.prompt_audio_tokens`);
        }
        if (costPerMultiple.completion_audio > 0) {
            costParts.push(`${costPerMultiple.completion_audio} * ${table}.completion_audio_tokens`);
        }
        if (costPerMultiple.prompt_cache_write > 0) {
            costParts.push(`${costPerMultiple.prompt_cache_write} * ${table}.prompt_cache_write_tokens`);
        }
        if (costPerMultiple.prompt_cache_read > 0) {
            costParts.push(`${costPerMultiple.prompt_cache_read} * ${table}.prompt_cache_read_tokens`);
        }
        if (costPerMultiple.image > 0) {
            costParts.push(`${costPerMultiple.image}`); // Assuming image cost is per image, not per token
        }
        if (costPerMultiple.per_call > 0) {
            costParts.push(`${costPerMultiple.per_call}`); // Assuming per_call cost is per call
        }
        const costString = costParts.length > 0 ? costParts.join(" + ") : "0";
        if (cost.model.operator === "equals") {
            const op = optimized ? "=" : "ILIKE";
            return `WHEN (${table}.model ${op} '${cost.model.value}') THEN ${costString}`;
        }
        else if (cost.model.operator === "startsWith") {
            return `WHEN (${table}.model LIKE '${cost.model.value}%') THEN ${costString}`;
        }
        else if (cost.model.operator === "includes") {
            return `WHEN (${table}.model ILIKE '%${cost.model.value}%') THEN ${costString}`;
        }
        else {
            throw new Error("Unknown operator");
        }
    });
    return `
  CASE
  ${validCases.join("\n")}
  ELSE ${useDefaultCost ? `toInt64(${table}.cost)` : `toInt64(0)`}
END
`;
}
// Current only used for backfilling costs via admin.
function clickhouseModelFilter(rows, table = "request_response_rmt") {
    const uniqueProviders = Array.from(new Set(rows.map((row) => row.provider)));
    return `
  (
    (${rows
        .map((row) => {
        if (row.modelRow.model.operator === "equals") {
            return `${table}.model = '${row.modelRow.model.value}'`;
        }
        else if (row.modelRow.model.operator === "startsWith") {
            return `${table}.model LIKE '${row.modelRow.model.value}%'`;
        }
        else if (row.modelRow.model.operator === "includes") {
            return `${table}.model ILIKE '%${row.modelRow.model.value}%'`;
        }
        else {
            throw new Error("Unknown operator");
        }
    })
        .join(" OR ")})
    AND
    (${table}.provider IN (${uniqueProviders.map((provider) => `'${provider}'`).join(",")}))
  )
  `;
}
// Currently only used for backfilling costs via admin.
// If not chunked, the query will be too large for Clickhouse.
function clickhousePriceCalcNonAggregated(models, table = "request_response_rmt") {
    const modelsByProvider = models.reduce((acc, model) => {
        if (!acc[model.provider]) {
            acc[model.provider] = [];
        }
        acc[model.provider].push(model.modelRow);
        return acc;
    }, {});
    return `
  (
    CASE
    ${Object.entries(modelsByProvider)
        .map(([provider, modelRows]) => {
        return `WHEN (${table}.provider = '${provider}') 
      THEN (${caseForCost(modelRows, table, costCalc_1.COST_PRECISION_MULTIPLIER)})`;
    })
        .join("\n    ")}
    ELSE 0
    END
  )
`;
}
function clickhousePriceCalc(table, inDollars = true) {
    const providersWithCosts = mappings_1.providers.filter((p) => p.costs && mappings_1.defaultProvider.provider !== p.provider);
    if (!mappings_1.defaultProvider.costs) {
        throw new Error("Default provider does not have costs");
    }
    return `
sum(
  CASE
  ${providersWithCosts
        .map((provider) => {
        if (!provider.costs) {
            throw new Error("Provider does not have costs");
        }
        return `WHEN (${table}.provider = '${provider.provider}') THEN (${caseForCost(provider.costs, table, costCalc_1.COST_PRECISION_MULTIPLIER)})`;
    })
        .join("\n")}
    ELSE ${caseForCost(mappings_1.defaultProvider.costs, table, costCalc_1.COST_PRECISION_MULTIPLIER)}
  END
  ) ${inDollars ? `/ ${costCalc_1.COST_PRECISION_MULTIPLIER}` : ""}
`;
}
